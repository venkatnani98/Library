import React from 'react';


const LoadingSpinner = (e) => {
  
  return (
    <div>
      <p>Loading.....</p>
    {/* <img
      src={spinner}
      style={{ width: '100px', margin: 'auto', display: 'block' }}
      alt="Loading..."
    /> */}
  </div>
  );
};

export default LoadingSpinner;
